import React from "react"

const ShowcaseItem = ({ text }) => {
  return (
    <div className='showcaseItem'>
      <span className='showcaseItemText'>{text}</span>
    </div>
  )
}

export default ShowcaseItem
