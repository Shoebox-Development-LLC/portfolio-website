import React from 'react'

// markup

const NavItem = ({ id }) => {
    return (
        <div className="navItem" id={id}>
            haha it can be blank now
        </div>
    )
}

export default NavItem
